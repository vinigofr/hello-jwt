const error = require('./error');
const validTheFields = require('./validTheFields');
const validUsername = require('./validUsername');
const validPassword = require('./validPassword');
const adminUser = require('./adminUser');

module.exports = {
  error,
  validTheFields,
  validUsername,
  validPassword,
  adminUser,
};
